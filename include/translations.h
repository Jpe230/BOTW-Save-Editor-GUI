#ifndef TRANSLATIONS_H_   /* Include guard */
#define TRANSLATIONS_H_

const char * translate(const char* rawName);
int getIDNumber(const char* rawName);
const char * translateMods(long int md);

#endif // TRANSLATIONS_H_